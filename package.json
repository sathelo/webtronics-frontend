{
  "name": "webtronics-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --host=localhost --port=8000",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "api": "json-server --watch __mocks__/index.json --host=localhost --port=3000",
    "ts-check": "vue-tsc -p tsconfig.json --noEmit --skipLibCheck"
  },
  "devDependencies": {
    "@ant-design-vue/nuxt": "^1.4.1",
    "@pinia/nuxt": "^0.5.1",
    "nuxt": "^3.9.0",
    "pinia": "^2.1.7",
    "vue": "^3.4.6",
    "vue-router": "^4.2.5"
  },
  "dependencies": {
    "json-server": "^1.0.0-alpha.19",
    "nuxt-icons": "^3.2.1",
    "sass": "^1.69.7"
  }
}
